Overlay Light
```jsx
  <div style={{height: 120, width: '100%', position: 'relative'}}>
    <LoadingOverlay light/>
  </div>
```

Overlay Dark
```jsx
  <div style={{height: 120, width: '100%', position: 'relative'}}>
    <LoadingOverlay />
  </div>
```